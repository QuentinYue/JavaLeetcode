/*public class ForOnlineTest {
    ListNode pre = null;
    ListNode next = null;
        while(head != null){
        next = head.next;
        head.next = pre;
        pre = head;
        head = next;
    }
        return pre; //pre最后会指向原 ll 的 null 之前的最后一个节点,即反转后 ll 的头节点.
}*/
